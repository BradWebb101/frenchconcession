import styled from 'styled-components'; 

export const Button = styled.div` 
   display: flex;
   justify-content:center; 
   align-items:center; 
   width: 100%; 
   left: 50%; 
   height: 40px; 
   font-size: 3rem; 
   z-index: 1; 
   cursor: pointer; 
   color: grey; 
`